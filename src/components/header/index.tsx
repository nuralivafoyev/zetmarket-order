import { FC } from 'react';
import './_style.scss';

const index: FC = () => {
  return (
    <header>
      <input type="search" className="search" placeholder="Search" />
    </header>
  );
};

export default index;
// export default App;
